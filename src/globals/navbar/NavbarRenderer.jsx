import React from 'react';
import Navbar from './Navbar';

const NavbarRenderer = () => {
    return (
        <header>
            <Navbar />
        </header>
    );
}

export default NavbarRenderer;
